import { Container, Typography } from "@mui/material";
import React from "react";

export const Kovriki = () => {
    return (
        <Container maxWidth="xs">
            <Typography py={"20px"} variant="h2">Коврики</Typography>
        </Container>
    )
}