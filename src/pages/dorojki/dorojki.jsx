import { Container, Typography } from "@mui/material";
import React from "react";

export const Dorojki = () => {
    return (
        <Container maxWidth="xs">
            <Typography py={"20px"} variant="h2">Дорожки</Typography>
        </Container>
    )
}