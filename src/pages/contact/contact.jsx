import { Container, Typography } from "@mui/material";
import React from "react";

export const Contact = () => {
    return (
        <Container maxWidth="xs">
            <Typography py={"20px"} variant="h2">Контакты</Typography>
        </Container>
    )
}